# Laravel vendor & node
/vendor
/node_modules

# Laravel storage (ignore contents but keep dirs)
/public/storage
/storage/*.key
/storage/logs/*
/storage/framework/cache/data/*
/storage/framework/sessions/*
/storage/framework/views/*
!storage/framework/cache/data/.gitignore
!storage/framework/sessions/.gitignore
!storage/framework/views/.gitignore

# Bootstrap cache (ignore files, keep folder)
bootstrap/cache/*.php
!bootstrap/cache/.gitignore

# Environment & secrets
.env
.env.*
Homestead.yaml
Homestead.json

# PHPUnit
.phpunit.result.cache
.phpunit.cache

# IDE/editor junk
.idea
.vscode
.phpstorm.meta.php
.DS_Store
Thumbs.db
*.swp
*.swo
*.log

# Keep lock files (track them in Git for reproducible builds)
# composer.lock
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Docker local-only files (but keep docker/ folder)
/docker-compose.override.yml
/db_data/
